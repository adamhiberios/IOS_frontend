// Auth shell — layout pieces used by /auth/* routes (login, register, …).
// These are presentational; auth state lives in core/auth.

export { AuthHeader } from './auth-header/auth-header';
export { AuthFooter } from './auth-footer/auth-footer';
